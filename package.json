{
  "name": "polymer-starter-kit",
  "license": "BSD-3-Clause",
  "devDependencies": {
    "bower": "^1.8.0",
    "eslint": "^3.12.0",
    "eslint-config-google": "^0.7.1",
    "eslint-plugin-html": "^1.7.0",
    "polymer-cli": "^0.17.0"
  },
  "scripts": {
    "lint": "./node_modules/.bin/eslint . --ext js,html --ignore-path .gitignore",
    "test": "npm run lint && ./node_modules/.bin/polymer test",
    "start": "./node_modules/.bin/polymer serve"
  }
}
